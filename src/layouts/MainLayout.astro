---
import "../styles/global.css";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es-MX">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="theme-color" content="#FCFBF9" />
    <meta name="description" content="PWA de control de accesos para Nauka Nayarit." />
    <link rel="manifest" href="/manifest.webmanifest" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Playfair+Display:wght@500;600&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body class="min-h-screen bg-[#FCFBF9] sunlight-text">
    <main class="mx-auto max-w-5xl px-4 py-6 sm:px-8">
      <header class="nauka-card-aero mb-6 px-5 py-4">
        <p class="sunlight-subtext text-sm font-semibold uppercase tracking-[0.14em]">Nauka Nayarit</p>
        <h1 class="font-display mt-1 text-2xl sm:text-3xl">{title}</h1>
      </header>
      <slot />
    </main>
  </body>
</html>
